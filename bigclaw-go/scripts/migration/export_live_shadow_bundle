#!/usr/bin/env bash
set -euo pipefail

script_dir="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
go_root="$(CDPATH= cd -- "${script_dir}/../.." && pwd)"

cd "${go_root}"
exec go run ./cmd/bigclawctl automation migration export-live-shadow-bundle "$@"
