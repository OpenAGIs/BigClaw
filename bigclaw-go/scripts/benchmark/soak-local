#!/usr/bin/env bash
set -euo pipefail

script_dir=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
go_root=$(cd -- "$script_dir/../.." && pwd)

cd "$go_root"
exec go run ./cmd/bigclawctl automation benchmark soak-local "$@"
